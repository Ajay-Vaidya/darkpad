/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Darkpad from './app/src/index';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Darkpad);
